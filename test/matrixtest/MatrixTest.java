package matrixtest;

import components.matrix.Matrix;

public abstract class MatrixTest {

    protected abstract Matrix<Integer> constructorTest();

    protected abstract Matrix<Integer> constructorRef();

}
